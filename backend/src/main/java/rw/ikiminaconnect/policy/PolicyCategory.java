package rw.ikiminaconnect.policy;

/** Mirrors src/lib/types.ts's RolePolicy['category']. */
public enum PolicyCategory {
    membership, savings, shares, loan, guarantor, suspension, exit, privacy
}
